# 舞影纪 · 技术说明

> 最近更新：2026-08-03。本文以当前仓库代码为准，若代码变更请同步更新。

## 一、项目概览

「舞影纪」是重庆市群众舞蹈艺术音像资料馆官方小程序，采用微信原生框架（WXML + WXSS + JavaScript），UI 基于腾讯 TDesign MiniProgram v1.15.3。

- 项目名：舞影纪 / DanceVerse
- 主体：重庆市群众舞蹈艺术音像资料馆
- 框架：微信小程序原生，不使用 Vue / React
- AppID：`wx4895e57dbf4857b7`
- 云开发环境：`cloudbase-d7ghywwf561a6e410`（仅用于代理云函数）
- 页面：11 个（其中 4 个 Tab 页）
- 组件库：tdesign-miniprogram `^1.15.3`（通过 npm 构建到 `miniprogram_npm`）
- 后端：Cloudflare Worker + D1 + R2，小程序侧保留一个云函数 `proxy` 做转发

## 二、页面与路由

`app.json` 页面清单：

| 页面 | 职责 | 数据来源 |
|---|---|---|
| `pages/index/index` | 首页 Tab：Banner 轮播、公告、9 个栏目导航、精品推荐 | `data/content.js` |
| `pages/groups/groups` | 舞团 Tab：37 个区县搜索与区域筛选 | `app.globalData.districts` |
| `pages/discover/discover` | 发现 Tab：最新活动 / 赛事集锦 / 旅游花絮 | `data/content.js` |
| `pages/profile/profile` | 我的 Tab：头像昵称登录、收藏/历史/点赞入口 | `wx.Storage`（`wyj_user` 等） |
| `pages/detail/detail` | 栏目详情：9 个栏目，政策栏目使用折叠面板 | `data/content.js` 的 `CATEGORY_DETAIL` |
| `pages/group-detail/group-detail` | 艺术团详情：节目目录 + 演出视频 | 页面内节目数组 + `data/videos.js` |
| `pages/troupes/troupes` | 某区县的艺术团列表 | 页面内置 `DISTRICT_TROUPES` + 兜底生成 |
| `pages/player/player` | 视频播放：原生 video、相关视频切换 | URL 参数传递 |
| `pages/list/list` | 通用列表：我的收藏 / 观看记录 / 点赞记录 | `TYPE_CONFIG` + `wx.Storage` |
| `pages/settings/settings` | 设置：关于、清除缓存、用户协议、隐私政策 | 静态配置 |
| `pages/info/info` | 文本页：关于 / 用户协议 / 隐私政策 | 页面内置文案 |

## 三、UI 与设计体系

- 主题色「红岩红」`#C41E2A`；页面背景 `#FFF5F5`；导航栏默认红底白字。
- `app.wxss` 引入 `miniprogram_npm/tdesign-miniprogram/common/style/index.wxss`，并定义全局工具类与品牌按钮。
- 模板中实际使用 19 种 TDesign 组件：`icon`、`tag`、`divider`、`button`、`skeleton`、`collapse`、`collapse-panel`、`tabs`、`tab-panel`、`cell`、`cell-group`、`grid`、`grid-item`、`image`、`search`、`empty`、`back-top`、`notice-bar`、`dialog`。（`avatar` 在 profile.json 中注册但当前模板未使用。）
- 首页 Banner 用原生 `swiper` + CSS `linear-gradient` 实现，无图片资源。

## 四、用户身份与登录

1. 启动时 `wx.login()` 获取 code。
2. 通过云函数 `proxy` 调用 Worker `GET /api/getOpenid?code=xxx`，Worker 再调微信 `jscode2session` 换取 openid。
3. openid 写入本地 `wyj_openid`；失败时生成匿名 `wyj_uid`（时间戳 + 随机数），保证未登录也能使用。
4. 头像昵称采用 `chooseAvatar` + `type="nickname"` 的合规方案，资料保存在本地 `wyj_user`。

## 五、数据存储方案

### 本地存储（7 个 Key）

| Key | 用途 |
|---|---|
| `wyj_user` | 头像昵称与统计信息 |
| `wyj_favorites` | 收藏列表 |
| `wyj_history` | 观看历史（LRU，上限 100） |
| `wyj_likeHistory` | 点赞历史（LRU，上限 100） |
| `wyj_myLikes` | 已点赞页面映射 |
| `wyj_openid` | 微信 openid |
| `wyj_uid` | 匿名降级身份 |

### 云端存储

- 点赞计数：Cloudflare D1 数据库 `wyj-likes` 的 `likes` 表（`page_id` + `count`）。
- 视频文件：Cloudflare R2，公开域名 `https://pub-261668b6483a438f8f09a549c5c0e4d1.r2.dev`，URL 由 `data/videos.js` 的 `getUrl()` 统一生成。
- 栏目/活动/赛事等内容仍为前端硬编码，集中在 `data/content.js`；`data/art_troupes.md` 为艺术团名录参考资料，暂未直接接入页面。

## 六、后端服务与调用链路

调用链路：小程序 `app.js` 的 `api()` → `wx.cloud.callFunction('proxy')` → 云函数转发 → Cloudflare Worker → D1 / 微信接口。

代理云函数位置：`cloudfunctions/proxy/index.js`，目标地址 `https://wyj-api.touhou31415.workers.dev`。

Cloudflare Worker API（`cloudflare/worker.js`）：

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/getOpenid?code=xxx` | GET | 换取微信 openid |
| `/api/likes?id=xxx` | GET | 查询点赞数 |
| `/api/likes/toggle` | POST | 原子增减点赞，body 为 `{ id, delta }`，`delta` 为 1 或 -1 |
| `/api/likes/total` | GET | 全站累计点赞数 |

保留云函数中转的原因：真机环境下微信严格校验 request 合法域名，`workers.dev` 域名不在白名单内；云函数运行在腾讯云侧，不受该限制。

## 七、并发与数据安全

- D1 使用 `INSERT OR IGNORE` 初始化行，再以 `UPDATE likes SET count = MAX(0, count + ?) WHERE page_id = ?` 原子增减，避免并发丢失与负数。
- 小程序侧采用「本地先更新 → 云端再同步」的两阶段策略，提供即时 UI 反馈。
- 所有云调用均有 try-catch，失败时返回安全默认值（点赞数 0 等），页面不会崩溃。

## 八、缓存与性能

- 观看历史与点赞历史使用 LRU 淘汰，上限 100 条。
- 栏目详情页用 TDesign `skeleton` 骨架屏覆盖 200ms 数据加载间隙。
- 内容数据通过 `data/*.js` 集中管理，页面只消费导出值，便于后续迁移云数据库。

## 九、技术选型决策

| 决策点 | 选择 | 原因 |
|---|---|---|
| 页面框架 | 微信原生 | 最小依赖、最高兼容 |
| 组件库 | TDesign | 腾讯官方、图标丰富、微信兼容 |
| 后端 | Cloudflare Worker + D1 | 免费额度充足、原子操作安全 |
| 视频存储 | Cloudflare R2 | 免费额度大、零带宽费 |
| 点赞计数 | D1 SQL 原子更新 | `MAX(0, count+delta)` 防负数 |
| 用户数据 | 本地存储 | 快速响应、离线可用 |
| 缓存策略 | LRU 100 条 | 平衡存储与实用性 |

## 十、项目规模

- 页面：11 个（4 Tab + 7 子页面）
- 页面源码：44 个文件（11 页 × 4）
- 核心源码：50 个文件（页面 44 + 全局 3 + 数据 3）
- TDesign 组件：模板实际使用 19 种
- 后端：Cloudflare Worker × 1 + 云函数 `proxy` × 1 + D1 数据库 + R2 存储
- 本地存储 Key：7 个

## 十一、关键技术细节

- 骨架屏使用二维数组 `rowCol` 描述布局，逐行匹配真实页面结构。
- `detail` 页通过 `isCollapse` 标志一页两用：政策栏目（id=4）走折叠面板，其余 8 个栏目走普通列表。
- 登录弹窗遮罩用 `catchtap` 阻止冒泡，避免误触页面其他事件。
- 通用列表页通过 URL `?type=favorites|history|likes` 复用同一页面。
- 播放页通过 URL 参数携带当前视频与相关视频列表，相关视频可原位切换。
- web-view 只能用于企业/政府主体，个人账号无法配置业务域名，这是平台策略限制。

## 十二、工程配置要点

- `project.config.json`：AppID `wx4895e57dbf4857b7`，`compileType: miniprogram`，`cloudfunctionRoot: cloudfunctions/`，公共基础库 `3.3.4`。
- `project.private.config.json`：本地基础库 `3.17.0`，开发期 `urlCheck: false`（关闭合法域名校验）。
- `cloudflare/wrangler.toml`：D1 绑定 `wyj-likes`，`WX_APPID` 已配置，`WX_SECRET` 需以 secret 方式注入。
- 部署 Worker 后需在微信公众平台配置 request 合法域名，或保留云函数 `proxy` 中转。

## 十三、已知待办 / 不一致

- `pages/info/info.js` 的隐私政策文案仍写着「点赞数存储在微信云开发数据库中」，与当前 D1 实现不一致。
- 首页精品推荐、发现页赛事/旅游卡片等图片暂用占位 `t-image`，未绑定真实图片地址。
- `data/videos.js` 目前只有 1 个视频配置了 R2 文件，其余为占位数据。
